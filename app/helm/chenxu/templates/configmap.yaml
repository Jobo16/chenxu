apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "chenxu.fullname" . }}-teams
  labels:
    {{- include "chenxu.labels" . | nindent 4 }}
data:
  teams.yaml: |
    {{- .Values.teamsConfig | default "" | nindent 4 }}
